%html
  %head
    %title Holler
    = stylesheet_link_tag 'reset', 'holler'
    = javascript_include_tag :defaults
  %body
    #header
      = render :partial => "users/user_bar"
    - if flash[:notice]
      #flash=flash[:notice]
    #content= yield